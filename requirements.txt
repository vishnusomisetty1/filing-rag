openai>=1.40        # OpenAI-compatible client for the local vLLM endpoint (no API key)
chromadb>=1.0
rank-bm25>=0.2.2
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31
streamlit>=1.40
# optional: RERANKER=cross-encoder
# sentence-transformers>=3.0
